import styled from "styled-components";

import { Wrapper } from "components";

// ### ### ### RECEIPT ### ### ###
export const StyledReceipt = styled.main`
  padding: 0.5rem;
`;

// ### ### ### RECEIPT WRAPPER ### ### ###
export const StyledReceiptWrapper = styled(Wrapper)``;
